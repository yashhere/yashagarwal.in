{
  "installCommand": "dnf install -y $(cat chrome-dependencies.txt) && bun install",
  "buildCommand": "bun run build",
  "rewrites": [
    {
      "source": "/stats/:match*",
      "destination": "https://umami.yashagarwal.in/:match*"
    }
  ]
}
