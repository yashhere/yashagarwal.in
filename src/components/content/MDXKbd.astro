---
import { cn } from "../../lib/utils"

interface Props {
  class?: string
  [key: string]: any
}

const { class: className, ...props } = Astro.props
---

<kbd
  class={cn(
    "border-border bg-muted text-foreground rounded border px-1.5 py-0.5 font-mono text-xs font-semibold shadow-[inset_0_-1px_0_0_hsl(var(--border))]",
    className
  )}
  {...props}
>
  <slot />
</kbd>
